{
  "name": "academy-finance-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Finance dashboard for academy-management.ru",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/auth.js && node --check src/database.js"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
